{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "http://milendall.com/schemas/room.json",

    "description": "This is the output of the brick-instantiation step, for a gate. It represents a single gate. There is as many files as there are gates",

    "properties": {
        "g_id" : {
            "type": "string",
            "description": "Unique gate ID"
        },
        "parameters" : { "$ref": "#parameters.json" }
    },
    "required": [
        "g_id",
        "parameters"
    ],
    "additionalProperties": false
}

